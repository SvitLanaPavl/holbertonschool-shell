#!/usr/bin/env bash
#Tell the story of passwd
while IFS=: read -r username gid homedir shell passwd comment; do
	echo "The user $username is part of the $gid gang, lives in $homedir and rides $shell. $username's place is protected by the passcode $passwd, more info about the user here: $comment"
done < /etc/passwd
